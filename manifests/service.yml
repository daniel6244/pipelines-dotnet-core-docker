apiVersion: v1
kind: Service
metadata:
    name: danielpipelinesdotnetcoredocker
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: danielpipelinesdotnetcoredocker